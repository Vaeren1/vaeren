from django.apps import AppConfig


class PflichtunterweisungConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "pflichtunterweisung"
    verbose_name = "Pflichtunterweisung (tenant)"
